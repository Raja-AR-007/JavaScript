//                          import a default export
// without {} curlybraces when use "export default"

import subract from "134-export default"  // without {} curlybraces

var b=subract(7,4);
console(b);